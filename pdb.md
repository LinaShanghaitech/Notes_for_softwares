## PDB
[参考链接](https://www.cnblogs.com/ilovewindy/p/6054923.html)
```
    where(w)
    list(l)
    list <line number>
    list <line n1, line n2>  显示制定开始行到结束行
    !运行python命令，比如!test=‘hello’, 将会把test变量改为hello
    next 运行至下行代码
    print(p)
    break(b)  显示断点情况
    break <line number>
    disable <break number>  将指定断点失效（但存在）
    enable <break number>   将指定断点生效
    continue
    return  运行至return 处
    clear
    condition <break number> <condition> 设置断点的触发条件
    ignore <break number> <n> 忽略断点n次
```
